<script>
console.log('je suis widget')
    const WidgetService = {
        
        /**
         * Set the magic number.
         * @param {Array<{id:string, x:number,y:number,w:number,h:number}>} widgetsData - The magic number.
         */
        updateWidgetsPositionAndSize:(widgetsData)=>{
            return fetch('/api/widget/updatePositionAndSize', {
                method:'POST',
                body:JSON.stringify(widgetsData), 
                headers:{
                     "Content-Type": "application/json",
                }
            })
        }
    }
</script>